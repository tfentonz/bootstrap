# bootstrap

## Install

### brew

* `brew install ack`
* `brew install tmux`
* `brew install tree`

**binaries**

* `brew cask install macvim`
* `brew cask install mysqlworkbench`
* `brew cask install skype`
